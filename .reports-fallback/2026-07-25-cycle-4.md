# crt nightly batch — 2026-07-25 (cycle 4 of 6)

*(Archive. Fourth cycle on this date, kept as its own file since 2026-07-25's
sixth cycle — see that cycle's report for why nesting them in one file broke
the inline-reply channel. **The current report is `2026-07-25.md`; replying
here reaches nobody.**)*

**Headline:** your inline reply pointed at the mid-utterance duck, which the
third cycle had already fixed (`f13c7a4`) — so I re-verified that fix rather
than re-doing it, and the audit found the hole *next* to it still open: ducked
audio was still reaching whisper through the **pre-roll** buffer. Measured, not
inferred — the utterance handed to whisper opened at exactly the playback
amplitude. Fixed. Then finished the sweep the previous cycle registered and
left undone, which turned out to be live: the test suite was printing headers
for two tests **that do not exist** and reporting ALL GREEN, while two test
files that *do* exist were never run at all. Three commits pushed. `ssh potato`
still blocked; `.claude/` gated for a fourth cycle — and this time I stopped
reporting that and built the tier a file it can write.

Commits (all pushed to `origin/main` = `mandark-lan:/home/zach/git-remotes/crt.git`):

| commit | what |
|---|---|
| `b37bbad` | keep ducked audio out of the pre-roll, not just out of the utterance |
| `ad41f5a` | stop the test suite claiming coverage it does not run |
| `d09a37b` | give the batch tier a notes file it can actually write |

Revert any with `git revert <sha>`. `tests/run_tests.sh` green after each.

---

## 1. Your reply, and the hole beside the fix — `b37bbad`

Your note quoted the "Observation, deliberately not changed" bullet about the
VAD checking `MUTED` only at utterance onset. That text lives in the preserved
*second*-cycle section of the report; the third cycle acted on it and landed
`f13c7a4`. So the first thing I did was check that fix is real rather than take
the report's word for it: `test_duck_midutterance_excision.sh` drives the real
CLI and still measures 300cs where the pre-fix code gave 400cs, and the
truncating alternative would give 110cs. It holds.

**What was still open is the same leak one buffer earlier.** The pre-roll deque
(`CRT_VAD_PREROLL`, 3 chunks) exists to prepend the moments *before* onset,
because a first word's attack sits under the VAD threshold. `pre.append(data)`
ran unconditionally — ducked or not. So:

| path | our own playback reaches whisper? |
|---|---|
| utterance *starts* during playback | no — start gate, always checked `MUTED` |
| duck arrives *mid*-utterance | no — since `f13c7a4` (freeze + excise) |
| playback sits in the pre-roll when the speaker starts | **yes, until this commit** |

Reachable on the live default path, in the flow the stability bar cares most
about: `addressed` (`CRT_EARCON_ON_ADDRESSED`, default **ON**) fires
immediately after `emit()` — exactly when a speaker carries on into a follow-up
utterance. At `CRT_VAD_PREROLL=3` the exposure is the ~100ms after the duck
lifts; the knob widens it linearly.

Witness: `tests/test_duck_preroll_leak.sh` drives the real CLI and measures the
**peak amplitude of the first 100ms of the WAV handed to whisper**, not its
duration — playback is emitted at 0.9 and speech at 0.3, so this is a 3x
separation rather than a chunk-counting argument that timing jitter could flip.
Against the parent commit it reports **29490**, which is exactly the playback
level; after the fix, **0**. The test raises `CRT_VAD_PREROLL` to 8 on purpose,
so the verdict does not hinge on winning a one-chunk race between the CTL line
and the audio it refers to.

The fix is `if not MUTED: pre.append(data)` — which splices the pre-roll across
the duck the same way `utt_chunk()` splices the utterance body. What leads the
utterance becomes the room tone from just before playback started.

`STT-MECHANISM.md` gained the matching paragraph: a follow-up in older
`stt.log` entries that picked up a phantom leading word now has a real
explanation that isn't "whisper misheard it".

**Stress-tested, one consequence worth knowing:** a *leaked* mute now also
freezes the pre-roll, so after `CRT_CTL_MUTE_MAX_SECS` (45s) force-clears one,
the next utterance gets a 300ms lead-in from 45s earlier. It's room tone, not
speech — ending an utterance needs 0.8s of trailing silence and the deque only
holds 0.3s, so it cannot be holding the tail of a word. Accepted.

## 2. The suite was claiming coverage it never ran — `ad41f5a`

The previous cycle wrote: *"a test that skips itself on a missing tool is an
exit-0 no-op… I did not sweep the rest of the repo for this pattern beyond the
`sox` cases."* I swept it. The general form was worse than the `sox` cases, and
it was active:

- **Named but absent.** `run_tests.sh` printed
  `== crt-audio-doctor.sh BUSY/DEAD/ERROR/LIVE verdicts ==` and
  `== crt-mic-footer.sh status-bar rendering ==`, ran nothing, and reported ALL
  GREEN. Both arrived with `38607bd` — one of the four potato cherry-picks —
  which brought the runner's *references* across without the test files. Each
  sat inside `if [ -f … ]; then`, so absence and success were indistinguishable,
  and the header printed directly above a *different* test's green output, so it
  read as covered.
- **Existing but unnamed.** `test_remote_claude_bridge.py` and
  `test_stt_confidence.py` were sitting in `tests/` and in no invocation — 18
  passing tests outside the suite, including the **only** coverage of the
  `CAPTURE`/`SEND` protocol behind the live brain-on-mandark path, which
  FOCUS.md calls the current top priority.

What landed:

- All 34 `[ -f "$DIR/test_X" ]` guards removed — every test is invoked
  unconditionally, so a file that goes missing fails loud. `test_secretary.py`
  keeps its named `CRT_SKIP_SECRETARY_TESTS` opt-out (a deliberate, *visible*
  skip) and loses only the `-f` half.
- `tests/test_audio_doctor.sh` written for real — 8 cases over
  `bin/crt-audio-doctor.sh`, which does exist and is real tooling. The case
  that matters: an `arecord` that fails with **no output at all** (device held
  by the sole reader) must read DEAD, not LIVE — the silent-success shape this
  project keeps hitting. Mutation-checked: stubbing the dead-peak comparison to
  `false` fails 3 of the 8, so these assertions have teeth.
- The header is now `LIVE/DEAD verdicts`. The script here emits two verdicts,
  not the four the inherited header claimed — see the open questions.
- `crt-mic-footer.sh` doesn't exist in this repo either, so its block is gone
  rather than left standing as a false claim.
- Both orphans wired in, both green.
- **A manifest check** at the end of `run_tests.sh`: every test file must be
  named, every named file must exist. Comment lines are stripped first, since
  the writeup itself names both missing files. Mutation-checked in both
  directions (planted an orphan → caught; renamed an invocation → caught). This
  is the mechanical enforcement rather than a comment asking the next person to
  remember.

## 3. `.claude/` gated for a fourth cycle — stopped reporting it, fixed it — `d09a37b`

The write to `.claude/QUESTIONS.md` was refused again. That's four consecutive
cycles, so it's a standing decision, not a glitch, and the skill's own
"keep `.claude/FOCUS.md` current" instruction has been unsatisfiable from this
tier the whole time.

`BATCH-NOTES.md` (repo root) is now the staging file this tier owns: what it
would have appended to `.claude/QUESTIONS.md`/`FOCUS.md` and could not.
Explicitly **not** a replacement — entries are to be folded into the real file
by a human or an ungated session and then deleted from there. It's tracked in
git rather than left in a report because a pending question needs to outlive a
per-day snapshot and travel with the code it's about, and pushing is the one
durable channel this tier actually has. `DEVELOPMENT-WORKFLOW.md`'s tier-1
description now names the gate and points at it, so the next batch instance
finds it instead of rediscovering the refusal.

This is the previous cycle's own suggestion ("this tier needs a backlog file
outside `.claude/` that it owns"), taken.

---

## Deferred / needs a human

- **`ssh potato` — still blocked, unchanged.** One probe this cycle:
  `ssh potato` fails at DNS (`Could not resolve hostname potato: Temporary
  failure in name resolution`) — still no `Host potato` block in this account's
  `~/.ssh/config`, despite FOCUS.md's 2026-07-23 claim that one was added.
  Corrections from previous cycles stand: `~/.ssh/id_ed25519.pub` does not
  exist in this account; `dexter_mandark_deploy.pub` is the natural key to
  authorize. Stopped after one probe per policy. I did **not** add a `Host`
  block myself — it wouldn't unblock anything (the key still isn't authorized)
  and silently pinning an IP that already moved once mid-move is how the last
  reachability incident started.
- **What a live session with potato would settle**, in priority order:
  1. `ls ~/crt/bin/crt-mic-footer.sh ~/crt/tests/test_mic_footer.sh` — one
     command, recovers whatever `38607bd` left behind (see §2 and the
     questions).
  2. `aplay -D default` and note which speaker it comes out of. Converts the
     third cycle's fail-safe guess into a decision.
  3. `CRT_WAKE_ARM_ENABLED=1`, talk in a natural multi-utterance burst, tune
     `CRT_WAKE_ARM_SECS` (12s) and `CRT_WAKE_ARM_MAX_SECS` (60s) by ear.
  4. Talk *through* an earcon with `CRT_EARCON_ON_THRESHOLD=1`: is the splice
     audible in the transcription, and is 2s right for
     `CRT_MUTE_UTT_MAX_SECS`?
  5. `python3 bin/crt-earcon-loopback-test.py handset` for bar item 2. Still
     evidence, not proof, without your own ear.
- **`.claude/SESSION-STATE.md` is inside the same gate**, so this cycle's
  findings are in `BATCH-NOTES.md` and this report only. The skill's startup
  instruction ("read SESSION-STATE.md first") still works; keeping it current
  from this tier does not.

## New issues found

- **"Our own playback must never reach whisper" had three doors, not two.**
  Start gate, utterance body, pre-roll. Two were closed and the third wasn't.
  Generalises: any future buffer that holds audio *across* a duck boundary
  needs the same treatment — the rule is "ducked frames never enter any buffer
  whisper will see", and it's now stated that way in `STT-MECHANISM.md`.
- **A cherry-pick can import a test *reference* without the test.** `38607bd`
  did it twice, and the `[ -f ]` guard made it invisible for the life of the
  repo since. The manifest check closes it, but the class is worth knowing for
  the *next* potato reconciliation — check that pulled runner lines have files
  behind them.
- **`bin/crt-stt-solo.py`'s ring path is a fourth audio producer with no duck,
  and it abandons an open utterance.** A `ring` CTL line spawns
  `aplay` with no `-D` (ALSA `default`) and the block ends in `continue`, which
  suppresses all VAD for the whole ring sequence. Two consequences: (a) if a
  ring arrives while `in_utt` is true, `buf` freezes and post-ring audio is
  later spliced onto pre-ring speech across a ~20s gap, with no
  `MUTE_UTT_MAX_SECS`-style bound, and (b) `pre` isn't appended during the
  ring, so the next utterance's lead-in comes from before it. **Not fixed**:
  `bin/crt-ring.sh` is the only writer and it is a hand-run tool, wired into
  neither `crt-console.sh` nor the idle-teaser, so fixing it would be fixing an
  unreached path — and the answer-detection design there (peak only checked in
  the `gap` phase, so the tone can't self-answer) is deliberate enough that I'd
  rather not reshape it blind. Registered in `BATCH-NOTES.md`.
- **`crt-sideband.sh` is not started by `crt-console.sh`**, so its continuous
  ambient tone — the one producer that would feed the mic *constantly* — is
  inert on the real boot path. Checked because it looked like the worst case;
  it isn't reachable. Recording it so the next cycle doesn't re-derive it.
- **The `| tail -5` pipelines in `run_tests.sh` are safe**, contrary to what
  they look like: `set -o pipefail` is on at the top, so a failing unittest
  still fails the suite. Checked because ~15 test files are invoked that way and
  a missing `pipefail` would have made every one of them an exit-0 no-op.

## Open questions (staged in `BATCH-NOTES.md`, still need folding into `.claude/QUESTIONS.md`)

1. **What did `bin/crt-mic-footer.sh` do, and does potato still have it?**
   Neither it nor its test ever existed here; `38607bd` imported only the
   runner's reference to the test. One `ls` over ssh answers it.
2. **Does potato's `crt-audio-doctor.sh` emit BUSY/ERROR verdicts?** The
   inherited header claimed four; the copy here emits two. If potato's really
   has four, that's a divergence to reconcile — I wrote the test against what
   this code does rather than fake the other two.
3. **Should this tier be allowed to write `.claude/`, or is `BATCH-NOTES.md`
   the answer?** Four cycles makes it a decision. Either grant the allowance,
   or bless the file and drop the `.claude/FOCUS.md` upkeep line from the skill
   so it stops asking for something that cannot happen.
4. **Is 2.0s right for `CRT_MUTE_UTT_MAX_SECS`?** Unchanged from last cycle —
   third by-ear number waiting on one live session, with `CRT_WAKE_ARM_SECS`
   (12s) and `CRT_WAKE_ARM_MAX_SECS` (60s).
5. **The `ssh potato` ask, unchanged** — which key/user should this account
   use? `dexter_mandark_deploy.pub` is on disk and ready to authorize.
